{{- range .Values.secrets }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .name }}
  labels:
    {{- include "proffy.labels" $ | nindent 4 }}
type: Opaque
data:
  {{- range $key, $value := .stringData }}
  {{ $key }}: {{ $value }}
  {{- end }}
---
{{- end }}