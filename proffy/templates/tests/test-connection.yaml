apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "proffy.fullname" . }}-test-connection"
  labels:
    {{- include "proffy.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "proffy.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
